trading_backend/
│
├── app/
│   ├── __init__.py
│   ├── main.py                     # Application entry point
│   ├── config.py                   # Configuration management
│   │
│   ├── core/                       # Core functionality
│   │   ├── __init__.py
│   │   ├── exceptions.py           # Custom exceptions
│   │   ├── security.py             # Password hashing, session management
│   │   └── dependencies.py         # FastAPI dependencies
│   │
│   ├── db/                         # Database layer
│   │   ├── __init__.py
│   │   ├── postgres.py             # PostgreSQL connection
│   │   └── redis.py                # Redis connection
│   │
│   ├── models/                     # SQLAlchemy models
│   │   ├── __init__.py
│   │   ├── base.py                 # Base model class
│   │   ├── user.py
│   │   ├── position.py
│   │   └── trade.py
│   │
│   ├── schemas/                    # Pydantic schemas
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── position.py
│   │   ├── trade.py
│   │   ├── auth.py
│   │   └── websocket.py
│   │
│   ├── repositories/               # Data access layer
│   │   ├── __init__.py
│   │   ├── base.py                 # Base repository
│   │   ├── user_repository.py
│   │   ├── position_repository.py
│   │   └── trade_repository.py
│   │
│   ├── services/                   # Business logic layer
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── user_service.py
│   │   ├── position_service.py
│   │   ├── price_service.py
│   │   ├── pnl_service.py
│   │   └── websocket_service.py
│   │
│   ├── api/                        # API routes
│   │   ├── __init__.py
│   │   ├── router.py               # Main router
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── auth.py
│   │       ├── users.py
│   │       ├── positions.py
│   │       └── websocket.py
│   │
│   ├── workers/                    # Background workers
│   │   ├── __init__.py
│   │   ├── binance_consumer.py     # Binance WebSocket consumer
│   │   └── pnl_calculator.py       # PnL calculation worker
│   │
│   └── middleware/                 # Custom middleware
│       ├── __init__.py
│       └── rate_limiter.py
│
├── migrations/                     # Alembic migrations
│   ├── versions/
│   └── env.py
│
├── tests/                          # Test files
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_auth.py
│   ├── test_positions.py
│   └── test_websocket.py
│
├── scripts/                        # Utility scripts
│   └── init_db.py
│
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── alembic.ini

===============================================================================================================
Design
===============================================================================================================

┌────────────────────────────────────────────────────────────────────────────────┐
│                           SYSTEM ARCHITECTURE                                   │
└────────────────────────────────────────────────────────────────────────────────┘

                                    EXTERNAL
                          ┌──────────────────────┐
                          │   Binance WebSocket  │
                          │    (Price Source)    │
                          └──────────┬───────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                         INGESTION LAYER                                       │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                    Price Ingestion Service                               │ │
│  │  • Single connection to Binance                                          │ │
│  │  • Buffers updates (100ms window)                                        │ │
│  │  • Publishes to Redis                                                    │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                           CACHING LAYER                                       │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                         Redis Cluster                                    │ │
│  │                                                                          │ │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │ │
│  │   │   Prices    │  │  Sessions   │  │  PnL Cache  │  │   Pub/Sub   │    │ │
│  │   │   Cache     │  │   Store     │  │             │  │  Channels   │    │ │
│  │   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                         APPLICATION LAYER                                     │
│                                                                               │
│    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│    │  FastAPI    │    │  FastAPI    │    │  FastAPI    │    │  FastAPI    │  │
│    │ Instance 1  │    │ Instance 2  │    │ Instance 3  │    │ Instance N  │  │
│    │             │    │             │    │             │    │             │  │
│    │ • REST API  │    │ • REST API  │    │ • REST API  │    │ • REST API  │  │
│    │ • WebSocket │    │ • WebSocket │    │ • WebSocket │    │ • WebSocket │  │
│    │ • PnL Calc  │    │ • PnL Calc  │    │ • PnL Calc  │    │ • PnL Calc  │  │
│    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                         PERSISTENCE LAYER                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                        PostgreSQL                                        │ │
│  │                                                                          │ │
│  │   Primary (Write)  ──────────────────►  Replica (Read)                   │ │
│  │                                                                          │ │
│  │   • Users          • Positions          • Trades History                 │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                                         │
│                                                                               │
│         ┌──────────┐  ┌──────────┐  ┌──────────┐       ┌──────────┐          │
│         │  User 1  │  │  User 2  │  │  User 3  │  ...  │ User 5000│          │
│         │ (React)  │  │ (React)  │  │ (React)  │       │ (React)  │          │
│         └──────────┘  └──────────┘  └──────────┘       └──────────┘          │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

=====================================================================================================
Price Update flow

┌─────────────────────────────────────────────────────────────────────────────┐
│                      PRICE UPDATE FLOW                                       │
└─────────────────────────────────────────────────────────────────────────────┘

Step 1: Price Arrives from Binance
─────────────────────────────────────────────────────────────────────────────
     Binance WS ──► "BTCUSDT: $67,234.50"
                          │
                          ▼
     ┌────────────────────────────────────────┐
     │         Price Ingestion Service        │
     │                                        │
     │  • DON'T process immediately           │
     │  • Add to buffer                       │
     │  • Buffer: {BTCUSDT: 67234.50, ...}    │
     └────────────────────────────────────────┘

Step 2: Buffer Flush (Every 100ms)
─────────────────────────────────────────────────────────────────────────────
     ┌────────────────────────────────────────┐
     │         Every 100 milliseconds:        │
     │                                        │
     │  1. Take all buffered prices           │
     │  2. Batch write to Redis               │
     │  3. Publish to Redis Pub/Sub           │
     │  4. Clear buffer                       │
     └────────────────────────────────────────┘
                          │
                          ▼
     WHY BUFFER? 
     ─────────────────────────────────────────
     Without buffer:  2,500 Redis writes/sec
     With buffer:     10 batch writes/sec (100ms interval)
     
     Reduction: 250x fewer operations!

Step 3: FastAPI Instances Receive Updates
─────────────────────────────────────────────────────────────────────────────
     Redis Pub/Sub ──► All FastAPI instances receive price updates
                          │
                          ▼
     Each instance:
     ┌────────────────────────────────────────┐
     │  1. Check which connected users hold   │
     │     positions in updated symbols       │
     │                                        │
     │  2. For affected users:                │
     │     • Calculate new PnL (from cache)   │
     │     • Send WebSocket message           │
     └────────────────────────────────────────┘

Step 4: User Receives Update
─────────────────────────────────────────────────────────────────────────────
     User's Browser ◄── WebSocket Message
                          │
                          ▼
     React updates UI instantly (no page refresh)

=====================================================================================================
Efficiency Analysis

┌─────────────────────────────────────────────────────────────────────────────┐
│                    EFFICIENCY ANALYSIS                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  NAIVE APPROACH (What NOT to do):                                            │
│  ────────────────────────────────                                            │
│  For each price update:                                                      │
│    → Write to database                          ❌ 2,500 DB writes/sec       │
│    → Query all users with this stock            ❌ 2,500 queries/sec         │
│    → Calculate PnL for each user                ❌ CPU overload              │
│    → Send individual WebSocket messages         ❌ Network overload          │
│                                                                              │
│  Result: System crashes under load                                           │
│                                                                              │
│  ═══════════════════════════════════════════════════════════════════════════ │
│                                                                              │
│  OUR APPROACH:                                                               │
│  ─────────────                                                               │
│  Buffer prices (100ms)                          ✅ 10 batch ops/sec          │
│  Store in Redis (not DB)                        ✅ Microsecond latency       │
│  Track user-symbol mapping in memory            ✅ O(1) lookup               │
│  Calculate PnL from cached prices               ✅ No DB reads               │
│  Batch WebSocket messages                       ✅ Efficient I/O             │
│                                                                              │
│  Result: Handles 10x the load with room to spare                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

====================================================================================================
🗄️ Database Design Philosophy
Why PostgreSQL + Redis (Not Just One)

┌─────────────────────────────────────────────────────────────────────────────┐
│                    DATABASE SELECTION RATIONALE                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────┐    ┌─────────────────────────────────┐
│         PostgreSQL              │    │            Redis                │
├──────��──────────────────────────┤    ├─────────────────────────────────┤
│                                 │    │                                 │
│  PURPOSE: Source of Truth       │    │  PURPOSE: Speed Layer           │
│                                 │    │                                 │
│  STORES:                        │    │  STORES:                        │
│  • User accounts                │    │  • Current prices (volatile)    │
│  • Position records             │    │  • Session tokens               │
│  • Trade history                │    │  • Calculated PnL (cached)      │
│  • Audit logs                   │    │  • User-symbol subscriptions    │
│                                 │    │                                 │
│  ACCESS PATTERN:                │    │  ACCESS PATTERN:                │
│  • Write: ~10-50/sec            │    │  • Write: ~1000/sec             │
│  • Read: ~100/sec (cached)      │    │  • Read: ~10,000/sec            │
│                                 │    │                                 │
│  LATENCY:                       │    │  LATENCY:                       │
│  • 5-50ms per query             │    │  • 0.1-1ms per operation        │
│                                 │    │                                 │
│  DURABILITY:                    │    │  DURABILITY:                    │
│  • ACID compliant               │    │  • Can lose data (acceptable    │
│  • Data survives crashes        │    │    for cache/prices)            │
│                                 │    │                                 │
└─────────────────────────────────┘    └─────────────────────────────────┘

                         WHY BOTH?
                         ─────────
        ┌────────────────────────────────────────────┐
        │                                            │
        │  Price updates are TEMPORARY:              │
        │  • Old price is useless once new arrives   │
        │  • No need to persist to disk              │
        │  • Redis: Perfect for this                 │
        │                                            │
        │  Positions are PERMANENT:                  │
        │  • Legal/audit requirements                │
        │  • Must survive system crashes             │
        │  • PostgreSQL: Perfect for this            │
        │                                            │
        │  Using wrong DB for wrong purpose:         │
        │  • Prices in PostgreSQL = Too slow         │
        │  • Positions in Redis = Data loss risk     │
        │                                            │
        └────────────────────────────────────────────┘

===============================================================================================
PostgreSQL Schema Design Principles

┌─────────────────────────────────────────────────────────────────────────────┐
│                    SCHEMA OPTIMIZATION STRATEGY                              │
└─────────────────────────────────────────────────────────────────────────────┘

PRINCIPLE 1: Index for Your Access Patterns
────────────────────────────────────────────

    Most common query: "Get all open positions for user X"
    
    WITHOUT proper index:
    ┌─────────────────────────────────────────┐
    │  SELECT * FROM positions                │
    │  WHERE user_id = ? AND status = 'OPEN'  │
    │                                         │
    │  Execution: Full table scan             │
    │  Time: 500ms (250,000 rows)             │
    │  With 5000 users: SERVER DIES           │
    └─────────────────────────────────────────┘
    
    WITH composite index on (user_id, status):
    ┌─────────────────────────────────────────┐
    │  Same query                             │
    │                                         │
    │  Execution: Index seek                  │
    │  Time: 2ms                              │
    │  With 5000 users: No problem            │
    └─────────────────────────────────────────┘

PRINCIPLE 2: Denormalize for Read Performance
─────────────────────────────────────────────

    Traditional normalized design:
    ┌──────────────┐      ┌──────────────┐
    │  positions   │      │    prices    │
    │  ──────────  │      │  ──────────  │
    │  symbol      │─────►│  symbol      │
    │  quantity    │      │  price       │
    │  entry_price │      │  updated_at  │
    └──────────────┘      └──────────────┘
    
    Problem: Need JOIN to calculate PnL = Slow
    
    Our design (partially denormalized):
    ┌──────────────────────────────────────┐
    │  positions                           │
    │  ────────────────────────────────    │
    │  symbol                              │
    │  quantity                            │
    │  entry_price                         │
    │  current_price     ◄── Updated by    │
    │  unrealized_pnl    ◄── background    │
    │                        process       │
    └──────────────────────────────────────┘
    
    Benefit: Single table read, no JOINs
    Trade-off: Slight data staleness (acceptable for our use case)

PRINCIPLE 3: Partition Hot Data
───────────────────────────────

    Positions table will grow forever as users trade.
    
    Solution: Separate OPEN and CLOSED positions
    
    ┌─────────────────────────────────────────────┐
    │  open_positions (Small, frequently accessed)│
    │  • Only current positions                   │
    │  • Maybe 250,000 rows                       │
    │  • Fast queries                             │
    └─────────────────────────────────────────────┘
                        │
                        │ When closed, move to:
                        ▼
    ┌─────────────────────────────────────────────┐
    │  position_history (Large, rarely accessed)  │
    │  • All closed positions                     │
    │  • Millions of rows over time               │
    │  • Partitioned by date                      │
    └─────────────────────────────────────────────┘

====================================================================================
🚀 Redis Caching Strategy
What We Cache and Why

┌─────────────────────────────────────────────────────────────────────────────┐
│                        REDIS CACHING STRATEGY                                │
└─────────────────────────────────────────────────────────────────────────────┘

CACHE 1: Current Prices
───────────────────────
    Key Pattern:    price:{symbol}
    Example:        price:BTCUSDT → {"price": 67234.50, "timestamp": "..."}
    TTL:            60 seconds
    Update Rate:    Every 100ms (batched)
    
    WHY CACHE THIS?
    • Price lookups happen thousands of times per second
    • Source (Binance) only sends updates, not queryable
    • Memory usage: ~500 symbols × 100 bytes = 50KB (trivial)


CACHE 2: User Portfolio PnL
───────────────────────────
    Key Pattern:    pnl:{user_id}
    Example:        pnl:user123 → {positions: [...], total_pnl: 1234.56}
    TTL:            5 seconds
    Update Rate:    On price change affecting user's positions
    
    WHY CACHE THIS?
    • Calculation requires: positions + current prices
    • Without cache: 50 positions × price lookup = 50 operations per request
    • With cache: 1 operation per request
    • Cache hit rate: ~80% (most requests within 5 second window)


CACHE 3: User Sessions
──────────────────────
    Key Pattern:    session:{user_id}:{session_id}
    Example:        session:user123:abc-def → {created_at: "...", device: "..."}
    TTL:            30 minutes (sliding)
    
    WHY CACHE THIS?
    • Every API request needs authentication
    • JWT validation is CPU-intensive
    • Session in Redis: O(1) lookup, instant validation
    • Also enables: instant logout, session management


CACHE 4: User-Symbol Subscriptions (Critical!)
──────────────────────────────────────────────
    Key Pattern:    user_symbols:{user_id}
                    symbol_subscribers:{symbol}
    
    This is a TWO-WAY mapping:
    
    ┌─────────────────────────────────────────────────────────────────┐
    │                                                                 │
    │  user_symbols:user123 = {BTCUSDT, ETHUSDT, BNBUSDT}            │
    │  user_symbols:user456 = {BTCUSDT, ADAUSDT}                      │
    │                                                                 │
    │  symbol_subscribers:BTCUSDT = {user123, user456, user789, ...}  │
    │  symbol_subscribers:ETHUSDT = {user123, user999, ...}           │
    │                                                                 │
    └─────────────────────────────────────────────────────────────────┘
    
    WHY THIS STRUCTURE?
    
    When BTCUSDT price updates:
    1. Look up symbol_subscribers:BTCUSDT → Get all affected users
    2. For each user, recalculate PnL and send WebSocket message
    
    Time complexity: O(1) to find affected users
    Without this: Would need to scan all 250,000 positions!

====================================================================================================
Cache Invalidation Strategy

┌─────────────────────────────────────────────────────────────────────────────┐
│                    CACHE INVALIDATION RULES                                  │
└─────────────────────────────────────────────────────────────────────────────┘

"There are only two hard things in Computer Science: 
 cache invalidation and naming things." - Phil Karlton

Our approach: TIME-BASED + EVENT-BASED hybrid

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   PRICE CACHE:                                                               │
│   ────────────                                                               │
│   Invalidation: TIME-BASED (60 second TTL)                                   │
│   Reason: Prices update so frequently that explicit invalidation             │
│           would cause more overhead than just letting them expire            │
│                                                                              │
│   PNL CACHE:                                                                 │
│   ──────────                                                                 │
│   Invalidation: EVENT-BASED + TIME-BASED                                     │
│   Events that invalidate:                                                    │
│     • User opens position    → Delete pnl:{user_id}                          │
│     • User closes position   → Delete pnl:{user_id}                          │
│     • Price update received  → Recalculate and update                        │
│   Fallback: 5 second TTL ensures eventual consistency                        │
│                                                                              │
│   SESSION CACHE:                                                             │
│   ──────────────                                                             │
│   Invalidation: EVENT-BASED                                                  │
│   Events that invalidate:                                                    │
│     • User logs out          → Delete session:{user_id}:{session_id}         │
│     • Password change        → Delete all sessions for user                  │
│     • Admin force logout     → Delete specific session                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

=====================================================================================================
🔌 WebSocket Architecture
Why WebSocket for This System

┌─────────────────────────────────────────────────────────────────────────────┐
│                    WEBSOCKET VS POLLING ANALYSIS                             │
└─────────────────────────────────────────────────────────────────────────────┘

OPTION 1: HTTP Polling
──────────────────────
    Client polls every 1 second for updates
    
    Requests per second = 5,000 users × 1 req/sec = 5,000 req/sec
    
    Each request:
    ├── TCP handshake (if not keep-alive)
    ├── TLS handshake (HTTPS)
    ├── HTTP request headers (~500 bytes)
    ├── HTTP response headers (~500 bytes)
    ├── Authentication check
    └── Response payload
    
    Problems:
    ├── Massive overhead for small updates
    ├── 1 second delay minimum (not real-time)
    ├── 90% of requests return "no change"
    └── Server CPU wasted on redundant auth checks


OPTION 2: WebSocket (Our Choice)
────────────────────────────────
    Single persistent connection per user
    
    Connections: 5,000 (constant, not per-second)
    
    Each update:
    ├── 2-byte frame header
    ├── Payload only (no HTTP overhead)
    └── No re-authentication needed
    
    Benefits:
    ├── True real-time (< 100ms latency)
    ├── 100x less bandwidth
    ├── Server pushes only when data changes
    └── Bi-directional (client can send commands too)
    

    BANDWIDTH COMPARISON:
    ┌────────────────────────────────────────────────────────┐
    │                                                        │
    │  Polling (5000 users, 1 req/sec, 1KB per req/resp):    │
    │  5000 × 1 × 2KB = 10 MB/second = 864 GB/day            │
    │                                                        │
    │  WebSocket (5000 users, avg 10 updates/sec, 100B):     │
    │  5000 × 10 × 100B = 5 MB/second = 432 GB/day           │
    │  Plus: Only sends when data changes!                   │
    │  Real-world: ~50 GB/day                                │
    │                                                        │
    └────────────────────────────────────────────────────────┘

===============================================================================
Connection Management Design

┌─────────────────────────────────────────────────────────────────────────────┐
│                    WEBSOCKET CONNECTION MANAGEMENT                           │
└─────────────────────────────────────────────────────────────────────────────┘

CHALLENGE: Managing 5,000+ concurrent connections

SOLUTION: Connection Manager Pattern
────────────────────────────────────

    ┌─────────────────────────────────────────────────────────────────────┐
    │                     Connection Manager                               │
    │  ─────────────────────────────────────────────────────────────────  │
    │                                                                      │
    │  Data Structures:                                                    │
    │                                                                      │
    │  active_connections: {                                               │
    │      "user123": [WebSocket1, WebSocket2],  # User can have multiple │
    │      "user456": [WebSocket3],              # devices connected      │
    │      ...                                                             │
    │  }                                                                   │
    │                                                                      │
    │  symbol_subscribers: {                                               │
    │      "BTCUSDT": {"user123", "user456", "user789"},                  │
    │      "ETHUSDT": {"user123", "user999"},                             │
    │      ...                                                             │
    │  }                                                                   │
    │                                                                      │
    │  Operations:                                                         │
    │                                                                      │
    │  connect(user_id, websocket):                                        │
    │      • Limit connections per user (prevent abuse)                    │
    │      • Add to active_connections                                     │
    │      • Load user's positions                                         │
    │      • Subscribe to relevant symbols                                 │
    │                                                                      │
    │  disconnect(user_id, websocket):                                     │
    │      • Remove from active_connections                                │
    │      • Clean up subscriptions if no connections left                 │
    │      • Free resources                                                │
    │                                                                      │
    │  broadcast_to_symbol(symbol, message):                               │
    │      • Get all subscribers: O(1)                                     │
    │      • Send to each user's connections: O(n) where n = subscribers  │
    │                                                                      │
    └─────────────────────────────────────────────────────────────────────┘


HANDLING MULTIPLE SERVER INSTANCES:
───────────────────────────────────

    Problem: With 3 FastAPI instances behind load balancer,
             user connections are distributed across instances.
             
    Instance 1 receives BTCUSDT price update, but user123 is
    connected to Instance 2. How to notify them?

    Solution: Redis Pub/Sub as Message Bus
    
    ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
    │ Instance 1  │     │ Instance 2  │     │ Instance 3  │
    │             │     │             │     │             │
    │ user123     │     │ user456 ◄───────── Price update │
    │ user789     │     │ user999     │     │ received    │
    └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
           │                   │                   │
           └───────────────────┼───────────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │    Redis Pub/Sub    │
                    │                     │
                    │ Channel: prices     │
                    │ All instances       │
                    │ subscribe to this   │
                    └─────────────────────┘
                               │
           ┌───────────────────┼───────────────────┐
           ▼                   ▼                   ▼
    ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
    │ Instance 1  │     │ Instance 2  │     │ Instance 3  │
    │             │     │             │     │             │
    │ Notify      │     │ Notify      │     │ (Originated │
    │ user123 ✓   │     │ user456 ✓   │     │  here)      │
    │ user789 ✓   │     │ user999 ✓   │     │             │
    └─────────────┘     └─────────────┘     └─────────────┘

===========================================================================================
📊 PnL Calculation Strategy
The Calculation Challenge

┌─────────────────────────────────────────────────────────────────────────────┐
│                    PNL CALCULATION CHALLENGE                                 │
└─────────────────────────────────────────────────────────────────────────────┘

For EACH user, PnL calculation requires:

    User's 50 positions:
    ┌─────────────────────────────────────────────────────────────────────┐
    │  Position 1: BTCUSDT, 0.5 BTC, entry $65,000                        │
    │  Position 2: ETHUSDT, 10 ETH, entry $3,200                          │
    │  Position 3: BNBUSDT, 50 BNB, entry $580                            │
    │  ... (47 more positions)                                             │
    └─────────────────────────────────────────────────────────────────────┘
    
    Current prices needed:
    ┌─────────────────────────────────────────────────────────────────────┐
    │  BTCUSDT: $67,234.50                                                │
    │  ETHUSDT: $3,456.78                                                 │
    │  BNBUSDT: $612.34                                                   │
    │  ... (prices for all held symbols)                                   │
    └─────────────────────────────────────────────────────────────────────┘
    
    Calculation per position:
    ┌─────────────────────────────────────────────────────────────────────┐
    │  LONG:  unrealized_pnl = quantity × (current_price - entry_price)   │
    │  SHORT: unrealized_pnl = quantity × (entry_price - current_price)   │
    │                                                                      │
    │  Example (Position 1):                                               │
    │  0.5 × ($67,234.50 - $65,000) = $1,117.25 profit                    │
    └─────────────────────────────────────────────────────────────────────┘


NAIVE APPROACH - Why It Fails:
──────────────────────────────

    On each price update (2,500/sec):
    
    1. Query database for affected users         ❌ 2,500 queries/sec
    2. Query database for their positions        ❌ 2,500 more queries/sec
    3. Calculate PnL for each position           ❌ CPU intensive
    4. Send to all affected users                ❌ Network intensive
    
    Database would need to handle: 5,000+ queries/second
    Result: Database becomes bottleneck, system grinds to halt


OUR APPROACH - Optimized Pipeline:
──────────────────────────────────

    Step 1: Batch price updates (100ms window)
    ─────────────────────────────────────────
        Instead of processing 2,500/sec individually,
        batch into 10 groups/sec
        
        Reduction: 250x fewer processing cycles
    
    Step 2: Use Redis for affected user lookup
    ──────────────────────────────────────────
        symbol_subscribers:BTCUSDT → {user123, user456, ...}
        
        O(1) lookup, no database query needed
    
    Step 3: Calculate PnL from cached data
    ──────────────────────────────────────
        For each affected user:
        • Get positions from cache (or DB if cache miss)
        • Get prices from Redis cache
        • Calculate in memory
        • Cache result for 5 seconds
        
    Step 4: Debounce per-user calculations
    ──────────────────────────────────────
        If BTCUSDT and ETHUSDT both update within 50ms,
        and user holds both:
        
        Without debounce: 2 separate PnL calculations
        With debounce: 1 combined calculation
        
        Reduction: Up to 50% fewer calculations

==========================================================================================
Calculation Flow Diagram

┌─────────────────────────────────────────────────────────────────────────────┐
│                    PNL CALCULATION FLOW                                      │
└─────────────────────────────────────────────────────────────────────────────┘

    Price Update: BTCUSDT = $67,234.50
                        │
                        ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │  1. LOOKUP AFFECTED USERS                                         │
    │                                                                   │
    │     Redis: SMEMBERS symbol_subscribers:BTCUSDT                    │
    │     Result: {user123, user456, user789, ...}                      │
    │     Time: < 1ms                                                   │
    └───────────────────────────────────────────────────────────────────┘
                        │
                        ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │  2. CHECK DEBOUNCE (per user)                                     │
    │                                                                   │
    │     If calculation already scheduled for user within 50ms:        │
    │       → Skip (will be included in pending calculation)            │
    │     Else:                                                         │
    │       → Schedule calculation                                      │
    └───────────────────────────────────────────────────────────────────┘
                        │
                        ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │  3. GATHER DATA (for each user)                                   │
    │                                                                   │
    │     Try cache first:                                              │
    │       Redis: GET pnl:user123                                      │
    │       If exists and fresh → Use cached value                      │
    │                                                                   │
    │     Cache miss:                                                   │
    │       Get positions from DB (only open positions)                 │
    │       Get all needed prices from Redis                            │
    └───────────────────────────────────────────────────────────────────┘
                        │
                        ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │  4. CALCULATE PNL                                                 │
    │                                                                   │
    │     For each position:                                            │
    │       pnl = quantity × (current_price - entry_price)              │
    │       pnl_percent = (pnl / invested) × 100                        │
    │                                                                   │
    │     Aggregate:                                                    │
    │       total_invested = Σ (quantity × entry_price)                 │
    │       total_pnl = Σ (individual pnl)                              │
    │       total_pnl_percent = (total_pnl / total_invested) × 100      │
    └───────────────────────────────────────────────────────────────────┘
                        │
                        ▼
    ┌───────────────────────────────────────────────────────────────────┐
    │  5. CACHE AND BROADCAST                                           │
    │                                                                   │
    │     Cache new PnL:                                                │
    │       Redis: SETEX pnl:user123 5 "{...}"                          │
    │                                                                   │
    │     Send to user via WebSocket:                                   │
    │       {                                                           │
    │         "type": "pnl_update",                                     │
    │         "data": {                                                 │
    │           "positions": [...],                                     │
    │           "total_pnl": 1234.56,                                   │
    │           "total_pnl_percent": 5.67                               │
    │         }                                                         │
    │       }                                                           │
    └───────────────────────────────────────────────────────────────────┘

===========================================================================================
🔐 Authentication Design
JWT Strategy

┌─────────────────────────────────────────────────────────────────────────────┐
│                    JWT AUTHENTICATION STRATEGY                               │
└─────────────────────────────────────────────────────────────────────────────┘

WHY JWT + REDIS (Hybrid Approach):
──────────────────────────────────

    Pure JWT (Stateless):
    ┌─────────────────────────────────────────────────────┐
    │  Pros:                                              │
    │  • No database lookup for validation                │
    │  • Scales infinitely                                │
    │                                                     │
    │  Cons:                                              │
    │  • Cannot invalidate tokens before expiry           │
    │  • Logout doesn't actually work                     │
    │  • Password change doesn't invalidate sessions      │
    └─────────────────────────────────────────────────────┘
    
    Pure Session (Stateful):
    ┌─────────────────────────────────────────────────────┐
    │  Pros:                                              │
    │  • Full control over sessions                       │
    │  • Instant invalidation                             │
    │                                                     │
    │  Cons:                                              │
    │  • Database lookup on every request                 │
    │  • 5,000 users × 10 req/sec = 50,000 lookups/sec   │
    └─────────────────────────────────────────────────────┘
    
    Our Hybrid Approach:
    ┌─────────────────────────────────────────────────────┐
    │                                                     │
    │  1. JWT contains user_id + session_id               │
    │  2. Session stored in Redis (not DB)                │
    │  3. On each request:                                │
    │     • Validate JWT signature (CPU only)             │
    │     • Check session exists in Redis (< 1ms)         │
    │                                                     │
    │  Benefits:                                          │
    │  • Fast validation (Redis, not PostgreSQL)          │
    │  • Can invalidate instantly (delete from Redis)     │
    │  • Scales well (Redis handles 100k+ ops/sec)        │
    │                                                     │
    └─────────────────────────────────────────────────────┘


TOKEN STRUCTURE:
───────────────

    Access Token (Short-lived: 15-30 minutes):
    ┌─────────────────────────────────────────────────────┐
    │  {                                                  │
    │    "sub": "user123",        // User ID              │
    │    "sid": "sess-abc-123",   // Session ID           │
    │    "type": "access",                                │
    │    "exp": 1699999999,       // Expiration           │
    │    "iat": 1699998888        // Issued at            │
    │  }                                                  │
    └─────────────────────────────────────────────────────┘
    
    Refresh Token (Long-lived: 7 days):
    ┌─────────────────────────────────────────────────────┐
    │  {                                                  │
    │    "sub": "user123",                                │
    │    "sid": "sess-abc-123",                           │
    │    "type": "refresh",                               │
    │    "exp": 1700604444                                │
    │  }                                                  │
    └─────────────────────────────────────────────────────┘


AUTHENTICATION FLOW FOR WEBSOCKET:
──────────────────────────────────

    Challenge: WebSocket doesn't support headers like HTTP
    
    Solution: Token in query parameter
    
    ┌─────────────────────────────────────────────────────┐
    │                                                     │
    │  1. Client connects:                                │
    │     ws://server/ws?token=eyJhbGciOiJIUzI1NiIs...    │
    │                                                     │
    │  2. Server validates token BEFORE accepting         │
    │                                                     │
    │  3. If valid:                                       │
    │     • Accept connection                             │
    │     • Associate with user_id                        │
    │     • Start sending updates                         │
    │                                                     │
    │  4. If invalid:                                     │
    │     • Reject connection                             │
    │     • Return 401 error                              │
    │                                                     │
    │  5. During connection:                              │
    │     • Periodically verify session still valid       │
    │     • Close if session invalidated                  │
    │                                                     │
    └─────────────────────────────────────────────────────┘
==============================================================================================
⚖️ Scalability Design
Horizontal Scaling Strategy

┌─────────────────────────────────────────────────────────────────────────────┐
│                    HORIZONTAL SCALING ARCHITECTURE                           │
└─────────────────────────────────────────────────────────────────────────────┘

LOAD BALANCER (NGINX):
──────────────────────

    ┌─────────────────────────────────────────────────────────────────────┐
    │                                                                      │
    │                        NGINX Load Balancer                           │
    │                                                                      │
    │  Responsibilities:                                                   │
    │  • SSL/TLS termination (offload crypto from app servers)            │
    │  • Rate limiting (protect against DDoS)                             │
    │  • Health checks (remove dead instances)                            │
    │  • WebSocket upgrade handling                                        │
    │  • Sticky sessions for WebSocket (optional)                         │
    │                                                                      │
    │  Configuration for WebSocket:                                        │
    │  ┌───────────────────────────────────────────────────────────────┐  │
    │  │  upstream backend {                                           │  │
    │  │      ip_hash;  # Same user goes to same server                │  │
    │  │      server backend1:8000;                                    │  │
    │  │      server backend2:8000;                                    │  │
    │  │      server backend3:8000;                                    │  │
    │  │  }                                                            │  │
    │  └───────────────────────────────────────────────────────────────┘  │
    │                                                                      │
    └─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                                                                      │
    │  ┌───────────┐    ┌───────────┐    ┌───────────┐    ┌───────────┐   │
    │  │ FastAPI 1 │    │ FastAPI 2 │    │ FastAPI 3 │    │ FastAPI N │   │
    │  │           │    │           │    │           │    │           │   │
    │  │ ~1,700    │    │ ~1,700    │    │ ~1,700    │    │   ...     │   │
    │  │ users     │    │ users     │    │ users     │    │           │   │
    │  └───────────┘    └───────────┘    └───────────┘    └───────────┘   │
    │                                                                      │
    │  Each instance:                                                      │
    │  • Handles subset of WebSocket connections                          │
    │  • Subscribes to Redis Pub/Sub for updates                          │
    │  • Stateless (can be replaced anytime)                              │
    │                                                                      │
    └─────────────────────────────────────────────────────────────────────┘


SCALING TRIGGERS:
─────────────────

    ┌─────────────────────────────────────────────────────────────────────┐
    │  Metric              │  Threshold   │  Action                       │
    ├─────────────────────────────────────────────────────────────────────┤
    │  CPU Usage           │  > 70%       │  Add instance                 │
    │  Memory Usage        │  > 80%       │  Add instance                 │
    │  WebSocket Conns     │  > 2000/inst │  Add instance                 │
    │  Request Latency     │  > 100ms p95 │  Add instance                 │
    │  Error Rate          │  > 1%        │  Investigate + maybe scale    │
    └─────────────────────────────────────────────────────────────────────┘


WHY THIS SCALES:
────────────────

    ┌─────────────────────────────────────────────────────────────────────┐
    │                                                                      │
    │  STATELESS APPLICATION LAYER:                                        │
    │  • Any request can go to any instance                               │
    │  • No session affinity required for REST                            │
    │  • Easy to add/remove instances                                      │
    │                                                                      │
    │  SHARED STATE IN REDIS:                                              │
    │  • All instances see same data                                       │
    │  • Pub/Sub ensures all instances get updates                        │
    │  • Redis Cluster for Redis scaling (if needed)                       │
    │                                                                      │
    │  DATABASE CONNECTION POOLING:                                        │
    │  • Each instance: 20 connections                                     │
    │  • 5 instances: 100 total connections                               │
    │  • PostgreSQL can handle 200+ connections easily                     │
    │                                                                      │
    └─────────────────────────────────────────────────────────────────────┘

============================================================================================
Scaling Bottleneck Analysis

┌─────────────────────────────────────────────────────────────────────────────┐
│                    BOTTLENECK ANALYSIS                                       │
└─────────────────────────────────────────────────────────────────────────────┘

COMPONENT          CAPACITY              BOTTLENECK AT        SOLUTION
─────────────────────────────────────────────────────────────────────────────

FastAPI            ~2000 WS/instance     10,000 users         Add more instances
Instance           ~5000 req/sec                              (horizontal scaling)

Redis              ~100,000 ops/sec      Rarely               Redis Cluster
(single)           ~10GB memory          (usually not         (if needed)
                                          the bottleneck)

PostgreSQL         ~5,000 queries/sec    20,000+ users        Read replicas
                   ~500 writes/sec       w/ heavy trading     Connection pooler
                                                              (PgBouncer)

Network            1 Gbps typical        Very high            Multiple NICs
                                         throughput           CDN for static

Binance WS         Unlimited (they       Never                N/A
                   handle it)

Your current load (5,000 users):
─────────────────────────────────────────────────────────────────────────────

    ┌─────────────────────────────────────────────────────────────────┐
    │                                                                 │
    │  FastAPI: 3 instances sufficient                                │
    │  • 5,000 ÷ 3 = ~1,700 WS connections each (well under limit)   │
    │                                                                 │
    │  Redis: Single instance sufficient                              │
    │  • ~5,000 ops/sec (5% of capacity)                             │
    │                                                                 │
    │  PostgreSQL: Single instance sufficient                         │
    │  • ~100 writes/sec peak (20% of capacity)                      │
    │  • ~500 reads/sec (mostly cached anyway)                        │
    │                                                                 │
    │  VERDICT: Comfortably handles 5,000 with room for 3-5x growth  │
    │                                                                 │
    └─────────────────────────────────────────────────────────────────┘

================================================================================================
🔢 Final Capacity Calculations

┌─────────────────────────────────────────────────────────────────────────────┐
│                    SYSTEM CAPACITY SUMMARY                                   │
└─────────────────────────────────────────────────────────────────────────────┘

GIVEN:
──────
    • 5,000 concurrent users
    • 50 positions per user
    • 5 price updates/second per stock
    • ~500 unique stocks being tracked

CALCULATIONS:
─────────────

    INCOMING PRICE UPDATES:
    ───────────────────────
    500 stocks × 5 updates/sec = 2,500 updates/sec from Binance
    
    With 100ms batching = 10 batches/sec
    Each batch: ~250 price updates
    
    
    REDIS OPERATIONS:
    ─────────────────
    Price writes:     10 batch writes/sec × 250 prices = 2,500 key updates/sec
    Price reads:      ~5,000/sec (PnL calculations)
    Session checks:   ~1,000/sec (API requests)
    PnL cache:        ~2,000/sec (read + write)
    ─────────────────────────────────────────
    Total:            ~10,000 ops/sec
    Redis capacity:   100,000 ops/sec
    Utilization:      10% ✓
    
    
    POSTGRESQL OPERATIONS:
    ──────────────────────
    Position reads:   Cache hit rate 80% → ~200 actual queries/sec
    Position writes:  ~50/sec (opens + closes during active trading)
    User queries:     ~100/sec
    ─────────────────────────────────────────
    Total:            ~350 queries/sec
    PostgreSQL capacity: 5,000 queries/sec
    Utilization:      7% ✓
    
    
    WEBSOCKET MESSAGES:
    ───────────────────
    Price broadcasts: 10/sec × 500 users affected avg = 5,000 messages/sec
    PnL updates:      ~2,000 messages/sec
    Heartbeats:       5,000 users ÷ 30 sec interval = ~170/sec
    ─────────────────────────────────────────
    Total:            ~7,200 messages/sec
    
    Per instance (3 instances): 2,400 messages/sec
    FastAPI WS capacity: 10,000+ messages/sec/instance
    Utilization:      24% ✓
    
    
    MEMORY USAGE:
    ─────────────
    Redis:
    • Prices: 500 × 100 bytes = 50 KB
    • Sessions: 5,000 × 500 bytes = 2.5 MB
    • PnL cache: 5,000 × 2 KB = 10 MB
    • Subscriptions: ~1 MB
    ─────────────────────────────────────────
    Total Redis: ~15 MB (trivial)
    
    FastAPI (per instance):
    • WebSocket connections: 1,700 × 10 KB = 17 MB
    • Connection manager: ~5 MB
    • Application: ~100 MB
    ─────────────────────────────────────────
    Total per instance: ~125 MB
    
    
LATENCY EXPECTATIONS:
─────────────────────

    ┌─────────────────────────────────────────────────────────────────┐
    │  Operation                    │  Expected Latency               │
    ├─────────────────────────────────────────────────────────────────┤
    │  Price → User's screen        │  100-200ms                      │
    │  (Binance → Buffer → Redis    │                                 │
    │   → PnL calc → WebSocket)     │                                 │
    ├─────────────────────────────────────────────────────────────────┤
    │  Open position API            │  50-100ms                       │
    ├─────────────────────────────────────────────────────────────────┤
    │  Close position API           │  50-100ms                       │
    ├─────────────────────────────────────────────────────────────────┤
    │  Get portfolio API            │  20-50ms (cached)               │
    │                               │  100-200ms (cache miss)          │
    └─────────────────────────────────────────────────────────────────┘


GROWTH CAPACITY:
────────────────

    Current design can handle (without major changes):
    
    ┌─────────────────────────────────────────────────────────────────┐
    │                                                                 │
    │  Users:         Up to 25,000 (5x current)                       │
    │  Positions:     Up to 1,000,000                                │
    │  Stocks:        Up to 2,000                                     │
    │                                                                 │
    │  To scale beyond:                                               │
    │  • Add more FastAPI instances (horizontal)                      │
    │  • Add PostgreSQL read replica                                  │
    │  • Use Redis Cluster                                            │
    │  • Consider dedicated PnL calculation service                   │
    │                                                                 │
    └─────────────────────────────────────────────────────────────────┘

==============================================================================================
✅ Why This Design Works

┌─────────────────────────────────────────────────────────────────────────────┐
│                    DESIGN SUCCESS FACTORS                                    │
└─────────────────────────────────────────────────────────────────────────────┘

1. RIGHT TOOL FOR RIGHT JOB
───────────────────────────
   • PostgreSQL: Durable data (positions, users, trades)
   • Redis: Fast ephemeral data (prices, cache, sessions)
   • WebSocket: Real-time streaming (not polling)
   
2. BATCHING & BUFFERING
───────────────────────
   • 100ms price buffer: 250x reduction in operations
   • Batch Redis writes: Pipeline multiple operations
   • Debounced PnL calc: Prevent redundant calculations
   
3. SMART CACHING
────────────────
   • Cache prices: Most frequently accessed data
   • Cache PnL: Expensive to calculate
   • Cache sessions: Every request needs auth
   • Appropriate TTLs: Fresh enough, not wasteful
   
4. EFFICIENT DATA STRUCTURES
────────────────────────────
   • Symbol→Users mapping: O(1) lookup for affected users
   • User→Connections mapping: O(1) for broadcasting
   • Indexed DB queries: O(log n) instead of O(n)
   
5. STATELESS APPLICATION
────────────────────────
   • No sticky sessions needed for REST
   • Any instance can handle any request
   • Easy horizontal scaling
   • Zero-downtime deployments
   
6. FAILURE RESILIENCE
─────────────────────
   • WebSocket reconnection logic
   • Redis reconnection handling
   • Database connection pooling
   • Graceful degradation (cache miss → DB fallback)

└─────────────────────────────────────────────────────────────────────────────┘

=================================================================================================
JWT + Session ID: Is It Necessary?
=================================================================================================


┌─────────────────────────────────────────────────────────────────┐
│                    THE REDUNDANCY PROBLEM                        │
└─────────────────────────────────────────────────────────────────┘

If we check Redis on EVERY request anyway:

    JWT + Session ID approach:
    ┌─────────────────────────────────────────┐
    │  1. Validate JWT signature (CPU work)   │
    │  2. Extract session_id from JWT         │
    │  3. Check Redis for session validity    │  ← Redis hit
    │  4. Proceed if valid                    │
    └─────────────────────────────────────────┘

    Pure Session ID approach:
    ┌─────────────────────────────────────────┐
    │  1. Check Redis for session validity    │  ← Same Redis hit
    │  2. Get user_id from Redis response     │
    │  3. Proceed if valid                    │
    └─────────────────────────────────────────┘

    Pure Session ID is SIMPLER and does the SAME job!

==============================================================================================
When JWT + Session Actually Makes Sense

┌─────────────────────────────────────────────────────────────────┐
│  SCENARIO                          │  BENEFIT OF JWT            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. SELECTIVE REDIS CHECKS                                       │
│     • Skip Redis for read-only, non-sensitive endpoints         │
│     • Only check Redis for writes/sensitive operations          │
│     • Trade-off: Delayed logout detection on some endpoints     │
│                                                                  │
│  2. MICROSERVICES ARCHITECTURE                                   │
│     • Service A validates JWT, calls Service B                  │
│     • Service B trusts JWT signature, skips Redis               │
│     • Reduces inter-service Redis dependencies                  │
│                                                                  │
│  3. REDUCED REDIS STORAGE                                        │
│     • JWT carries user_id, roles, permissions                   │
│     • Redis only stores: session_id → {exists: true}            │
│     • Smaller Redis memory footprint                            │
│                                                                  │
│  4. THIRD-PARTY API CONSUMERS                                    │
│     • External clients expect standard JWT format               │
│     • OAuth2/OpenID Connect compatibility                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

=================================================================================================
Recommendation

┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│   GO WITH PURE SESSION ID - It's simpler and sufficient         │
│                                                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                                                         │   │
│   │   Login:                                                │   │
│   │   → Generate: session_id = random UUID                  │   │
│   │   → Store: Redis SET session:{session_id} {user_data}   │   │
│   │   → Return: session_id as cookie or token               │   │
│   │                                                         │   │
│   │   Each Request:                                         │   │
│   │   → Redis GET session:{session_id}                      │   │
│   │   → Returns user_id, created_at, etc.                   │   │
│   │   → Single operation, simple logic                      │   │
│   │                                                         │   │
│   │   Logout:                                               │   │
│   │   → Redis DEL session:{session_id}                      │   │
│   │   → Instant invalidation                                │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│   WHY THIS IS FINE:                                              │
│   • Single monolithic FastAPI (no microservices)                │
│   • Always need real-time session validity (trading system)     │
│   • Redis lookup is < 1ms anyway                                │
│   • Less code, fewer bugs                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

=====================================================================================
Summary

┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│   JWT + Session ID = Overengineering for your use case          │
│                                                                  │
│   Pure Session ID = Same security, simpler implementation       │
│                                                                  │
│   The JWT hype makes people add it everywhere, but if you're    │
│   hitting Redis on every request anyway, JWT adds complexity    │
│   without adding value.                                         │
│                                                                  │
│   Use JWT when:                                                  │
│   • You can skip Redis checks sometimes                         │
│   • You have microservices needing distributed auth             │
│   • External APIs need standard token format                    │
│                                                                  │
│   Use Pure Session when:                                         │
│   • Single application                                          │
│   • Always need instant invalidation                            │
│   • Simplicity matters                                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

==============================================================================================
some tradeoff

┌─────────────────────────────────────────────────────────────────────────────┐
│                     ADD MESSAGE QUEUE FOR TRADES                             │
│                                                                              │
│  Current (Risky):                                                           │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐                             │
│  │  Client  │────►│  FastAPI │────►│  Postgres│                             │
│  └──────────┘     └──────────┘     └──────────┘                             │
│                         │                                                    │
│                         │ If FastAPI crashes here,                          │
│                         │ trade might be lost                                │
│                         ▼                                                    │
│                   ┌──────────┐                                              │
│                   │  Redis   │                                              │
│                   └──────────┘                                              │
│                                                                              │
│  Recommended:                                                                │
│  ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐           │
│  │  Client  │────►│  FastAPI │────►│  Redis   │────►│  Worker  │           │
│  └──────────┘     └──────────┘     │  Streams │     │          │           │
│       ▲                            └──────────┘     └────┬─────┘           │
│       │                                                  │                  │
│       │                            ┌──────────┐          │                  │
│       └────────────────────────────│  Postgres│◄─────────┘                  │
│                Confirmation        └──────────┘                             │
│                via WebSocket                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

===============================================================================================
🗄️ Database Design: Position vs Trade Tables
Why We Need Both Tables

┌─────────────────────────────────────────────────────────────────────────────┐
│                    POSITION vs TRADE: ARCHITECTURAL DECISION                 │
└─────────────────────────────────────────────────────────────────────────────┘

A common question: "Why do we have both Position and Trade tables? Isn't Position sufficient?"

The answer lies in understanding that these represent fundamentally different concepts:

┌─────────────────────────────────┐    ┌─────────────────────────────────┐
│         POSITION TABLE          │    │          TRADE TABLE            │
├─────────────────────────────────┤    ├─────────────────────────────────┤
│                                 │    │                                 │
│  PURPOSE: Current Holdings      │    │  PURPOSE: Transaction History   │
│                                 │    │                                 │
│  REPRESENTS:                    │    │  REPRESENTS:                    │
│  • What you currently own       │    │  • Individual buy/sell actions │
│  • Your market exposure         │    │  • Execution details           │
│  • Unrealized P&L              │    │  • Realized P&L                │
│                                 │    │                                 │
│  LIFECYCLE:                     │    │  LIFECYCLE:                     │
│  • Mutable (updates with trades)│    │  • Immutable (never changes)   │
│  • Can be closed/deleted        │    │  • Permanent audit trail       │
│                                 │    │                                 │
│  SIZE:                          │    │  SIZE:                          │
│  • Small (~50 per user)         │    │  • Large (unlimited growth)    │
│                                 │    │                                 │
│  ACCESS PATTERN:                │    │  ACCESS PATTERN:                │
│  • Frequent (real-time updates) │    │  • Occasional (reports/history)│
│                                 │    │                                 │
└─────────────────────────────────┘    └─────────────────────────────────┘

============================================================================================
Real-World Trading Example

Let's trace through a realistic trading scenario:

Day 1: Buy 0.5 BTC at $60,000
Day 2: Buy 0.3 BTC at $62,000  
Day 3: Sell 0.2 BTC at $65,000
Day 4: Buy 0.1 BTC at $63,000

TRADES TABLE (4 immutable records):
┌────┬─────────┬──────┬──────────┬────────┬──────────────┬─────────────┐
│ ID │ Symbol  │ Side │ Quantity │ Price  │ Executed_At  │ Realized_PnL│
├────┼─────────┼──────┼──────────┼────────┼──────────────┼─────────────┤
│ 1  │ BTCUSDT │ BUY  │ 0.5      │ 60,000 │ Day1 10:00   │ 0           │
│ 2  │ BTCUSDT │ BUY  │ 0.3      │ 62,000 │ Day2 14:30   │ 0           │
│ 3  │ BTCUSDT │ SELL │ 0.2      │ 65,000 │ Day3 09:15   │ +800        │
│ 4  │ BTCUSDT │ BUY  │ 0.1      │ 63,000 │ Day4 16:45   │ 0           │
└────┴─────────┴──────┴──────────┴────────┴──────────────┴─────────────┘

POSITION TABLE (1 mutable record):
┌─────────┬──────────┬─────────────────┬────────┬──────────────────┐
│ Symbol  │ Quantity │ Avg_Entry_Price │ Status │ Unrealized_PnL   │
├─────────┼──────────┼─────────────────┼────────┼──────────────────┤
│ BTCUSDT │ 0.7      │ 61,285.71      │ OPEN   │ (varies w/price) │
└─────────┴──────────┴─────────────────┴────────┴──────────────────┘

Calculation: (0.5×60k + 0.3×62k - 0.2×avg + 0.1×63k) ÷ 0.7 = 61,285.71

============================================================================================
Why Both Tables Are Essential

1️⃣ DIFFERENT DATA LIFECYCLES
─────────────────────────────

Position (Mutable):
```python
# Updates existing record with each trade
position.quantity += trade.quantity
position.avg_entry_price = calculate_weighted_average()
position.unrealized_pnl = calculate_current_pnl()
```

Trade (Immutable):
```python
# Creates new record, never modified
trade = Trade(
    side="BUY",
    quantity=0.5,
    price=60000,
    executed_at=datetime.now()
)
# This record NEVER changes - regulatory requirement
```

2️⃣ DIFFERENT QUERY PATTERNS
────────────────────────────

Position Queries (Real-time Performance):
```sql
-- Dashboard: "What do I currently own?"
SELECT * FROM positions 
WHERE user_id = ? AND status = 'OPEN'

-- WebSocket: "Calculate my current P&L"
SELECT symbol, quantity, entry_price 
FROM positions WHERE user_id = ?
```

Trade Queries (Historical Analysis):
```sql
-- Reports: "Show my trading history"
SELECT * FROM trades 
WHERE user_id = ? 
ORDER BY executed_at DESC

-- Tax: "Calculate realized gains for 2024"
SELECT SUM(realized_pnl) FROM trades 
WHERE user_id = ? AND YEAR(executed_at) = 2024
```

3️⃣ REGULATORY & COMPLIANCE REQUIREMENTS
────────────────────────────────────────

Financial regulations require:
✅ Complete audit trail of all transactions (Trades)
✅ Immutable transaction records
✅ Exact execution timestamps
✅ Fee tracking for cost basis
✅ Realized P&L calculations

Positions alone cannot provide this level of detail.

4️⃣ PERFORMANCE OPTIMIZATION
────────────────────────────

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  POSITIONS TABLE (Hot Data):                                                │
│  • Small size: ~50 records per user                                         │
│  • Frequent access: Every price update                                      │
│  • Fast queries: Indexed on (user_id, status)                              │
│  • Real-time P&L calculations                                               │
│                                                                              │
│  TRADES TABLE (Cold Data):                                                  │
│  • Large size: Millions of records over time                               │
│  • Infrequent access: Reports and analysis                                  │
│  • Partitioned by date for efficiency                                       │
│  • Historical analysis and compliance                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

5️⃣ COMPLEX TRADING SCENARIOS
─────────────────────────────

Scenario A: Partial Position Closing
```
Current Position: 1.0 BTC
Action: Sell 0.3 BTC at $65,000

Position Update: quantity = 0.7 BTC (modified existing record)
Trade Creation: SELL 0.3 BTC @$65,000 (new immutable record)
```

Scenario B: Average Price Calculation
```
Trade 1: BUY 0.5 BTC @$60,000
Trade 2: BUY 0.3 BTC @$62,000

Position: Shows weighted average = $60,750
Trades: Preserve individual execution prices for analysis
```

Scenario C: Tax Reporting
```
Need: All trades for the year with realized P&L
Position: Only shows current holdings
Trades: Complete transaction history with gains/losses
```

============================================================================================
Alternative Approach Analysis (Why It Fails)

If we tried using ONLY Positions table:

❌ PROBLEMS:
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  class Position:                                                             │
│      current_quantity: float                                                 │
│      avg_entry_price: float                                                  │
│      status: str                                                             │
│                                                                              │
│  But how do we track:                                                        │
│  • Individual trade timestamps? ❌                                           │
│  • Fees paid on each trade? ❌                                               │
│  • Exact execution prices? ❌                                                │
│  • Partial fills and order details? ❌                                       │
│  • Complete trading history for reports? ❌                                  │
│  • Realized P&L for tax purposes? ❌                                         │
│  • Compliance audit trail? ❌                                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

============================================================================================
Implementation in Our System

Our codebase correctly implements this pattern:

1️⃣ POSITION CREATION (app/services/position_service.py):
```python
# Create position for current holdings
position = await self.position_repo.create_position(
    user_id=user_id,
    symbol=data.symbol,
    quantity=float(data.quantity),
    entry_price=float(data.entry_price),
    position_type=PositionType(data.position_type.value)
)

# Create trade for transaction record
await self.trade_repo.create_trade(
    user_id=user_id,
    symbol=data.symbol,
    side=trade_side,
    quantity=float(data.quantity),
    price=float(data.entry_price),
    position_id=position.id  # Link them together
)
```

2️⃣ POSITION CLOSING:
```python
# Update position status
closed_position = await self.position_repo.close_position(
    position_id=position_id,
    exit_price=float(exit_price),
    realized_pnl=realized_pnl
)

# Record the closing trade
await self.trade_repo.create_trade(
    user_id=user_id,
    symbol=position.symbol,
    side=trade_side,
    quantity=float(position.quantity),
    price=float(exit_price),
    position_id=position_id,
    realized_pnl=realized_pnl  # Important for tax reporting
)
```

============================================================================================
Database Schema Relationships

┌─────────────────────────────────────────────────────────────────────────────┐
│                         TABLE RELATIONSHIPS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────┐         ┌──────────────┐         ┌─────────────┐
    │    USER     │         │   POSITION   │         │    TRADE    │
    │             │         │              │         │             │
    │ id (PK)     │◄────────┤ user_id (FK) │◄────────┤ user_id (FK)│
    │ email       │         │ symbol       │         │ symbol      │
    │ username    │         │ quantity     │         │ side        │
    │ ...         │         │ entry_price  │         │ quantity    │
    └─────────────┘         │ status       │         │ price       │
                            │ ...          │         │ executed_at │
                            │              │         │ realized_pnl│
                            │ id (PK)      │◄────────┤ position_id │
                            └──────────────┘         │ ...         │
                                                     └─────────────┘

Relationships:
• User → Position: One-to-Many (user can have multiple positions)
• User → Trade: One-to-Many (user can have many trades)
• Position → Trade: One-to-Many (position can have multiple trades)

============================================================================================
Performance Benefits

┌─────────────────────────────────────────────────────────────────────────────┐
│                         QUERY PERFORMANCE COMPARISON                         │
└─────────────────────────────────────────────────────────────────────────────┘

REAL-TIME DASHBOARD QUERY:
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  With Separate Tables (Our Approach):                                       │
│  SELECT * FROM positions WHERE user_id = ? AND status = 'OPEN'              │
│  • Scans: ~50 position records                                              │
│  • Time: 2-5ms                                                              │
│  • Index: (user_id, status) - highly selective                              │
│                                                                              │
│  With Single Table (Alternative):                                           │
│  SELECT symbol, SUM(quantity), AVG(price)                                   │
│  FROM trades WHERE user_id = ? AND position_status = 'OPEN'                 │
│  GROUP BY symbol                                                             │
│  • Scans: Thousands of trade records                                        │
│  • Time: 50-200ms                                                           │
│  • Complex aggregation on large dataset                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

HISTORICAL REPORT QUERY:
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  With Separate Tables (Our Approach):                                       │
│  SELECT * FROM trades WHERE user_id = ? ORDER BY executed_at DESC           │
│  • Direct access to trade history                                           │
│  • Time: 10-20ms                                                            │
│  • Clean, normalized data                                                   │
│                                                                              │
│  With Single Table (Alternative):                                           │
│  Would need complex queries to reconstruct trade history                    │
│  from position changes - not feasible                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

============================================================================================
Industry Standard Practice

This dual-table approach is used by:

🏦 TRADITIONAL FINANCE:
• Banks: Account balances (positions) + Transaction history (trades)
• Brokerages: Portfolio holdings + Order execution records
• Exchanges: Open positions + Trade matching records

🪙 CRYPTO EXCHANGES:
• Binance: Spot balances + Trade history
• Coinbase: Portfolio + Transaction records  
• Kraken: Positions + Order fills

📊 TRADING PLATFORMS:
• Interactive Brokers: Positions + Executions
• TD Ameritrade: Holdings + Trade confirmations
• E*TRADE: Portfolio + Transaction history

============================================================================================
Conclusion

✅ BOTH TABLES ARE ESSENTIAL because they serve different purposes:

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  POSITION TABLE:                                                             │
│  • Answers: "What do I own right now?"                                      │
│  • Used for: Real-time P&L, risk management, portfolio display              │
│  • Optimized for: Fast queries, frequent updates                            │
│                                                                              │
│  TRADE TABLE:                                                                │
│  • Answers: "What transactions have I made?"                                │
│  • Used for: Audit trails, tax reporting, performance analysis              │
│  • Optimized for: Data integrity, historical queries                        │
│                                                                              │
│  TOGETHER THEY PROVIDE:                                                      │
│  ✅ High-performance real-time trading                                       │
│  ✅ Complete regulatory compliance                                           │
│  ✅ Comprehensive audit trails                                               │
│  ✅ Flexible reporting capabilities                                          │
│  ✅ Scalable architecture                                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

Our implementation follows financial industry best practices and provides
the foundation for a robust, compliant, and scalable trading platform.

The separation of concerns between current state (positions) and historical
events (trades) is not just a design choice—it's a fundamental requirement
for any serious financial trading system.